name: "Release (macOS Universal)"
description: "Build macOS Universal binary - kept for reference, main logic is in release.yml"

runs:
  using: "composite"
  steps:
    - name: No-op
      shell: bash
      run: echo "Main workflow is in release.yml"
